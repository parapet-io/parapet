package io.parapet.instances

/** Aggregator trait for typeclass instances exposed by parapet.
  *
  * Currently a placeholder - instances live in backend modules. Mix in via [[io.parapet.instances.all]] for forward
  * compatibility.
  */
trait AllInstances
